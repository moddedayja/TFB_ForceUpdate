# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'FireBaseRemote' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FireBaseRemote
	pod 'Firebase/Core'
  pod 'Firebase/RemoteConfig'
  target 'FireBaseRemoteTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FireBaseRemoteUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
